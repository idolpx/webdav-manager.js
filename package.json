{
  "name": "webdav-manager",
  "version": "1.0.0",
  "private": true,
  "description": "WebDAV manager browser app",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
